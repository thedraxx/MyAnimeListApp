import styled from 'styled-components/native';

export const ContainerButton = styled.View`
  position: absolute;
  top: 0;
  padding: 5px;
  z-index: 1;
`;

export const TouchableButton = styled.TouchableOpacity`
  padding: 5px;
  margin: 0px;
  border-radius: 20px;
  background-color: '#ffffff0fff';
`;

export const ButtonText = styled.Text`
  font-size: 20px;
  font-weight: bold;
`;
